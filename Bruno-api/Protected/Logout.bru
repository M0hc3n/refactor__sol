meta {
  name: Logout
  type: http
  seq: 8
}

post {
  url: {{base_url}}/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.setEnvVar("token", "");
  }
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Logged out successfully", function() {
    expect(res.body.message).to.equal("Logged out successfully");
  });
}